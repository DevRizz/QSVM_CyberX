# Streamlit secrets are optional; Streamlit reads .streamlit/secrets.toml too.
# For this app, env is optional. You can leave this file as-is.

# Quantum kernel config
QK_WIRES=4
QK_LANDMARKS=64
QK_EMBED_SCALE=1.0

# Online buffer
BUFFER_MAX=512
BUFFER_MIN_TRAIN=64

# Database
DB_PATH=./anomaly_events.db

# Crypto (if set, overrides random derivation in BB84 test mode)
# QKD_SEED=optional_integer
